PORT=8787
AI_BASE_URL=https://api.openai.com/v1
AI_API_KEY=
AI_MODEL=
# A reasoning model thinks before it answers: a narrative patch can take a
# minute. This is the proxy's own ceiling; the browser has its own, in
# src/ai/checkpointQueue.ts. Nothing in the game waits on either — a timeout
# just yields the scripted beat instead.
AI_TIMEOUT_MS=120000
